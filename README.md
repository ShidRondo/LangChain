# LangChain
Final Requirements

Every time creating a new terminal. Create a virtual environment again. Make sure you're at the same folder (expected folder)
source venv/bin/activate

In using doc_qa_claude.py
to escape the prompt. Use "Exit"/"exit"